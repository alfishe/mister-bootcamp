[← Linux System](../README.md) · [↑ Knowledge Base](../../README.md)

# System Scripts

Shell scripts that orchestrate the MiSTer runtime: startup sequence, core switching, network configuration, USB device handling, and update mechanisms.

## Planned Articles

| File | Topic |
|---|---|
| `startup_sequence.md` | Init → MiSTer binary launch: `/etc/init.d/`, `S99user`, core autoboot |
| `core_switching.md` | How `MiSTer` binary reloads itself after FPGA reconfiguration |
| `network_scripts.md` | WiFi/Ethernet setup: `wpa_supplicant`, DHCP, static IP |
| `usb_mount.md` | USB storage auto-mount and game directory detection |
